  <!-- Get Involved Section -->
  <section class="cta-section">
    <div class="container">
      <div class="cta-content">
        <h2>Get Involved</h2>
        <p>Join us in preserving this natural treasure for future generations. Every action counts in protecting our precious reservoir.</p>
        <div class="cta-buttons">
          <a href="/volunteer" class="btn btn-primary">Memberships</a>
          <a href="/donate" class="btn btn-secondary">Donate</a>
          <a href="/contact" class="btn btn-outline">Volunteer</a>
        </div>
      </div>
    </div>
    <style>
          /* CTA Section */
  .cta-section {
    padding: var(--spacing-2xl) 0;
    background: var(--earth-gradient);
    text-align: center;
    color: white;
  }
  h2 {
    color:white !important;
  }

  .cta-content h2 {
    font-size: var(--font-size-3xl);
    font-weight: 700;
    margin: 0 0 var(--spacing-md) 0;
  }

  .cta-content p {
    font-size: var(--font-size-lg);
    max-width: 600px;
    margin: 0 auto var(--spacing-2xl) auto;
    color: var(--earth-sand);
  }

  .cta-buttons {
    display: flex;
    justify-content: center;
    gap: var(--spacing-md);
    flex-wrap: wrap;
  }

  .btn {
    display: inline-block;
    padding: var(--spacing-xs) var(--spacing-md);
    font-size: var(--font-size-sm);
    letter-spacing: 0.07em;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
    border: 2px solid transparent;
  }

  .btn-primary {
    background: var(--earth-clay);
    color: white;
  }

  .btn-primary:hover {
    background: #b56b4c;
    transform: translateY(-2px);
  }

  .btn-secondary {
    background: var(--earth-sage);
    color: white;
  }

  .btn-secondary:hover {
    background: #7a9760;
    transform: translateY(-2px);
  }

  .btn-outline {
    background: transparent;
    color: white;
    border-color: white;
  }

  .btn-outline:hover {
    background: white;
    color: var(--earth-deep-forest);
  }
    </style>